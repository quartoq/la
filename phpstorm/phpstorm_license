Лицензии phpstorm


Лицензии хранятся там же где и настройки, то есть в каталоге .PhpStorm для phpstorm для версии > 10 и каталогах .WebIdeN для phpstorm с соответствующей версией N (например, в каталоге .WebIde90 для 9 версии phpstorm, .WebIde80 - для 8 и т.д.).
Эти каталоги с настройками в зависимости от ностроек в файле PHPSTORM_HOME\bin\idea.properties хранятся обычно либо в профиле пользователя (например, C:\Users\test_user\ если пользователь test_user), либо в каталоге PHPSTORM_HOME.
Файл ключа лицензии phpstorm хранится в каталоге PHPSTORM_HOME\.PhpStorm\config\phpstorm.key для phpstorm > 10 и PHPSTORM_HOME\.WebIde\config\phpstormN0.key для phpstorm соответствующей версии N).

Файл ключа лицензии хранится в USC2 (2-байтный юникод) с меткой bom (в notepad++ покажет как яя).
Для каждого ключа (key, это как бы имя пользователя, логин) имеется свой ключ лицензии (License Key), который состоит из 4 строк с обрамлением двумя строками (всего 6 строк).

Для того, чтобы извлечь лицензию из phpstorm в текстовом виде, если она уже была введена, достаточно открыть из меню Help -> Register окно "PhpStorm License Activation", где можно будет просмотреть и скопировать ключ лицензии.

Существуют генераторы ключей для всех продуктов JetBrains, в том числе и для phpstorm (вероятно поэтому они и ввели активацию по сети).
